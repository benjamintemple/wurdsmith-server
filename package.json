{
  "name": "server",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "gql": "open http://localhost:4000/api && nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "dependencies": {
    "@types/node": "^18.7.3",
    "apollo-server-express": "^3.10.1",
    "dotenv": "^16.0.1",
    "express": "^4.16.3",
    "graphql": "^16.5.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
